# azure-maf-poc


python3 -m venv path/to/venv                                                              
source path/to/venv/bin/activate
pip install agent-framework azure-identity
pip install --pre agent-framework
pip install --pre agent-framework-gemini
pip install google-genai
